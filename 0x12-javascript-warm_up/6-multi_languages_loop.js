#!/usr/bin/node

// 6-multi_languages_loop.js

// Array containing the lines to be printed
const lines = ["C is fun", "Python is cool", "JavaScript is amazing"];

// Loop through the array and print each line using a single console.log
for (let i = 0; i < lines.length; i++) {
	console.log(lines[i]);
}
