#!/usr/bin/node

// 4-concat.js

// Get the two arguments passed to the script
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Concatenate the arguments in the specified format and print the result
console.log(`${arg1} is ${arg2}`);
